






> How iam Born

```bash
  $ mkdir kankuu && cd kankuu
  $ git init
  $ git config --local user.name kankuu
  $ git config --local user.email kankuu@mail.local
  $ git add . && git commit -m 'initial Life'
  $ git remote add mom $belly
  $ git push -u mom iam

  # acctualy, Life isn't so simple like this~
  # just keep on going, and you'll know d'answer.

```






